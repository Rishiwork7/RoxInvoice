{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "build": "esbuild index.js --bundle --platform=node --target=node18 --minify --outfile=dist/server.js --external:puppeteer --external:bullmq --external:ioredis --external:firebase-admin --external:express",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "bullmq": "^5.70.1",
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "firebase-admin": "^13.6.1",
    "helmet": "^8.1.0",
    "ioredis": "^5.9.3",
    "nodemailer": "^8.0.1",
    "puppeteer": "^24.37.5"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "nodemon": "^3.1.14"
  }
}